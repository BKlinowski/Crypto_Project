class BlowFish {
    /**
     * BlowFish encryption
     * @param {string} pt_string
     * @param {string} key_string
     * @returns {byte[]} ciphertext in bytes
     */
    static encrypt(pt_string, key_string) {
      return "BlowFish encryption (not yet implemented)";
    }
    /**
     * BlowFish decryption
     * @param {byte[]} ct_bytes
     * @param {string} key_string
     * @returns {string} plaintext as string
     */
    static decrypt(ct_bytes, key_string) {
        return "BlowFish decryption (not yet implemented)";
    }
  }
  
  export default BlowFish;
